export interface Course {
  // tslint:disable-next-line:typedef-whitespace
  id: number;
  description: string;
  iconUrl: string;
  longDescription: string;
  category: string;
  lessonCount: number;
}
